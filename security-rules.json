{
  "_说明": "在微信开发者工具「云开发控制台 → 数据库」中，为每个集合分别设置自定义安全规则（复制对应 read/write 内容）。云函数以管理员权限运行，不受这些规则限制。",
  "articles": {
    "read": "doc.status == 'published'",
    "write": false,
    "_含义": "所有人可读取已发布文章；客户端禁止直接写入，仅云函数可写"
  },
  "categories": {
    "read": true,
    "write": false,
    "_含义": "所有人可读取分类；客户端禁止写入"
  },
  "admins": {
    "read": false,
    "write": false,
    "_含义": "管理员 openid 白名单，客户端完全不可读写，仅 verifyAdmin 等云函数可访问"
  }
}
